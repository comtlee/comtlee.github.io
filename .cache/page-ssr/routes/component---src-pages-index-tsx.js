"use strict";
exports.id = 691;
exports.ids = [691];
exports.modules = {

/***/ 5907:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Z: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _emotion_react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(6657);
function _EMOTION_STRINGIFIED_CSS_ERROR__(){return"You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";}const defaultStyle= true?{name:"1k777wz",styles:"*{padding:0;margin:0;box-sizing:border-box;font-family:'Nanum Myeongjo',serif;}html,body,#___gatsby{height:100%;}a,a:hover{color:inherit;text-decoration:none;cursor:pointer;}"}:0;const GlobalStyle=function(){return (0,_emotion_react__WEBPACK_IMPORTED_MODULE_0__/* .jsx */ .tZ)(_emotion_react__WEBPACK_IMPORTED_MODULE_0__/* .Global */ .xB,{styles:defaultStyle});};/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (GlobalStyle);

/***/ }),

/***/ 1090:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  Z: () => (/* binding */ Common_Template)
});

// EXTERNAL MODULE: ./node_modules/@emotion/styled/base/dist/emotion-styled-base.esm.js + 1 modules
var emotion_styled_base_esm = __webpack_require__(5858);
// EXTERNAL MODULE: ./src/components/Common/GlobalStyle.tsx
var GlobalStyle = __webpack_require__(5907);
// EXTERNAL MODULE: ./node_modules/@emotion/react/dist/emotion-react.esm.js
var emotion_react_esm = __webpack_require__(6657);
;// CONCATENATED MODULE: ./src/components/Common/Footer.tsx
function _EMOTION_STRINGIFIED_CSS_ERROR__(){return"You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";}const FooterWrapper=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("footer", true?{target:"eunpi8n0"}:0)( true?{name:"9w9jmx",styles:"display:grid;place-items:center;margin-top:auto;padding:50px 0;font-size:15px;text-align:center;line-height:1.5;@media (max-width: 768px){font-size:13px;}"}:0);const Footer=function(){return (0,emotion_react_esm/* jsx */.tZ)(FooterWrapper,null,"Thank You for Visiting My Blog, Have a Good Day \uD83D\uDE06",(0,emotion_react_esm/* jsx */.tZ)("br",null),"\xA9 2024 Developer Comt, Powered By Gatsby.");};/* harmony default export */ const Common_Footer = (Footer);
// EXTERNAL MODULE: ./node_modules/react-helmet/es/Helmet.js
var Helmet = __webpack_require__(4593);
;// CONCATENATED MODULE: ./src/components/Common/Template.tsx
function Template_EMOTION_STRINGIFIED_CSS_ERROR_(){return"You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";}const Container=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("main", true?{target:"e15ow87l0"}:0)( true?{name:"zyllbl",styles:"display:flex;flex-direction:column;height:100vh"}:0);const Template=function({title,description,url,image,children}){return (0,emotion_react_esm/* jsx */.tZ)(Container,null,(0,emotion_react_esm/* jsx */.tZ)(Helmet.Helmet,null,(0,emotion_react_esm/* jsx */.tZ)("title",null,title),(0,emotion_react_esm/* jsx */.tZ)("meta",{name:"description",content:description}),(0,emotion_react_esm/* jsx */.tZ)("meta",{name:"viewport",content:"width=device-width, initial-scale=1.0"}),(0,emotion_react_esm/* jsx */.tZ)("meta",{httpEquiv:"Content-Type",content:"text/html;charset=UTF-8"}),(0,emotion_react_esm/* jsx */.tZ)("meta",{property:"og:type",content:"website"}),(0,emotion_react_esm/* jsx */.tZ)("meta",{property:"og:title",content:title}),(0,emotion_react_esm/* jsx */.tZ)("meta",{property:"og:description",content:description}),(0,emotion_react_esm/* jsx */.tZ)("meta",{property:"og:image",content:image}),(0,emotion_react_esm/* jsx */.tZ)("meta",{property:"og:url",content:url}),(0,emotion_react_esm/* jsx */.tZ)("meta",{property:"og:site_name",content:title}),(0,emotion_react_esm/* jsx */.tZ)("meta",{name:"twitter:card",content:"summary"}),(0,emotion_react_esm/* jsx */.tZ)("meta",{name:"twitter:title",content:title}),(0,emotion_react_esm/* jsx */.tZ)("meta",{name:"twitter:description",content:description}),(0,emotion_react_esm/* jsx */.tZ)("meta",{name:"twitter:image",content:image}),(0,emotion_react_esm/* jsx */.tZ)("meta",{name:"twitter:site",content:"comt"}),(0,emotion_react_esm/* jsx */.tZ)("meta",{name:"twitter:creator",content:"comt"}),(0,emotion_react_esm/* jsx */.tZ)("html",{lang:"ko"})),(0,emotion_react_esm/* jsx */.tZ)(GlobalStyle/* default */.Z,null),children,(0,emotion_react_esm/* jsx */.tZ)(Common_Footer,null));};/* harmony default export */ const Common_Template = (Template);

/***/ }),

/***/ 2978:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  "default": () => (/* binding */ pages)
});

// NAMESPACE OBJECT: ./node_modules/query-string/base.js
var base_namespaceObject = {};
__webpack_require__.r(base_namespaceObject);
__webpack_require__.d(base_namespaceObject, {
  exclude: () => (exclude),
  extract: () => (extract),
  parse: () => (parse),
  parseUrl: () => (parseUrl),
  pick: () => (pick),
  stringify: () => (stringify),
  stringifyUrl: () => (stringifyUrl)
});

// EXTERNAL MODULE: ./node_modules/@emotion/styled/base/dist/emotion-styled-base.esm.js + 1 modules
var emotion_styled_base_esm = __webpack_require__(5858);
// EXTERNAL MODULE: external "/Users/sang-a/Desktop/blog/node_modules/react/index.js"
var index_js_ = __webpack_require__(2553);
// EXTERNAL MODULE: ./src/components/Common/Template.tsx + 1 modules
var Template = __webpack_require__(1090);
// EXTERNAL MODULE: ./.cache/gatsby-browser-entry.js + 11 modules
var gatsby_browser_entry = __webpack_require__(7076);
// EXTERNAL MODULE: ./node_modules/@emotion/react/dist/emotion-react.esm.js
var emotion_react_esm = __webpack_require__(6657);
;// CONCATENATED MODULE: ./src/components/Main/CategoryList.tsx
function _EMOTION_STRINGIFIED_CSS_ERROR__(){return"You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";}const CategoryListWrapper=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"el024hc1"}:0)( true?{name:"7on4i6",styles:"display:flex;flex-wrap:wrap;width:768px;margin:100px auto 0;@media (max-width: 768px){width:100%;margin-top:50px;padding:0 20px;}"}:0);// eslint-disable-next-line @typescript-eslint/no-unused-vars
const CategoryItem=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)(({active,...props})=>(0,emotion_react_esm/* jsx */.tZ)(gatsby_browser_entry.Link,props), true?{target:"el024hc0"}:0)("margin-right:20px;padding:5px 0;font-size:18px;font-weight:",({active})=>active?'800':'400',";cursor:pointer;&:last-of-type{margin-right:0;}@media (max-width: 768px){font-size:15px;}"+( true?"":0));const CategoryList=function({selectedCategory,categoryList}){return (0,emotion_react_esm/* jsx */.tZ)(CategoryListWrapper,null,Object.entries(categoryList).map(([name,count])=>(0,emotion_react_esm/* jsx */.tZ)(CategoryItem,{to:`/?category=${name}`,active:name===selectedCategory,key:name},"#",name,"(",count,")")));};/* harmony default export */ const Main_CategoryList = (CategoryList);
// EXTERNAL MODULE: ./node_modules/gatsby-plugin-image/dist/gatsby-image.module.js
var gatsby_image_module = __webpack_require__(3040);
;// CONCATENATED MODULE: ./src/components/Main/ProfileImage.tsx
function ProfileImage_EMOTION_STRINGIFIED_CSS_ERROR_(){return"You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";}const ProfileImageWrapper=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)(gatsby_image_module/* GatsbyImage */.HN, true?{target:"eapcmkh0"}:0)( true?{name:"1on2x4e",styles:"width:120px;height:120px;margin-bottom:30px;border-radius:50%;@media (max-width: 768px){width:80px;height:80px;}"}:0);const ProfileImage=function({profileImage}){return (0,emotion_react_esm/* jsx */.tZ)(ProfileImageWrapper,{image:profileImage,alt:"Profile Image"});};/* harmony default export */ const Main_ProfileImage = (ProfileImage);
;// CONCATENATED MODULE: ./src/components/Main/Introduction.tsx
function Introduction_EMOTION_STRINGIFIED_CSS_ERROR_(){return"You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";}const Background=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"e7e6za13"}:0)( true?{name:"1u7ncg7",styles:"width:100%;background-image:linear-gradient(60deg, #29323c 0%, #485563 100%);color:#ffffff"}:0);const Wrapper=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"e7e6za12"}:0)( true?{name:"1kisa53",styles:"display:flex;flex-direction:column;justify-content:center;align-items:flex-start;width:768px;height:400px;margin:0 auto;@media (max-width: 768px){width:100%;height:300px;padding:0 20px;}"}:0);const SubTitle=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"e7e6za11"}:0)( true?{name:"1h8u8jj",styles:"font-size:20px;font-weight:400;@media (max-width: 768px){font-size:15px;}"}:0);const Title=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"e7e6za10"}:0)( true?{name:"1mgdphf",styles:"margin-top:5px;font-size:35px;font-weight:700;@media (max-width: 768px){font-size:25px;}"}:0);const Introduction=function({profileImage}){return (0,emotion_react_esm/* jsx */.tZ)(Background,null,(0,emotion_react_esm/* jsx */.tZ)(Wrapper,null,(0,emotion_react_esm/* jsx */.tZ)(Main_ProfileImage,{profileImage:profileImage}),(0,emotion_react_esm/* jsx */.tZ)("div",null,(0,emotion_react_esm/* jsx */.tZ)(Title,null,"Frontend Developer Comt"))));};/* harmony default export */ const Main_Introduction = (Introduction);
// EXTERNAL MODULE: ./node_modules/@babel/runtime/helpers/esm/extends.js
var esm_extends = __webpack_require__(7462);
;// CONCATENATED MODULE: ./src/components/Main/PostItem.tsx
function PostItem_EMOTION_STRINGIFIED_CSS_ERROR_(){return"You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";}const PostItemWrapper=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)(gatsby_browser_entry.Link, true?{target:"ersx7z77"}:0)( true?{name:"m7gxp6",styles:"display:flex;flex-direction:column;border-radius:10px;box-shadow:0 0 8px rgba(0, 0, 0, 0.15);transition:0.3s box-shadow;cursor:pointer;&:hover{box-shadow:0 0 10px rgba(0, 0, 0, 0.3);}"}:0);const ThumbnailImage=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)(gatsby_image_module/* GatsbyImage */.HN, true?{target:"ersx7z76"}:0)( true?{name:"1axbq5h",styles:"width:100%;height:200px;border-radius:10px 10px 0 0"}:0);const PostItemContent=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"ersx7z75"}:0)( true?{name:"1do7u82",styles:"flex:1;display:flex;flex-direction:column;padding:15px"}:0);const PostItem_Title=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"ersx7z74"}:0)( true?{name:"kw2b4d",styles:"display:-webkit-box;overflow:hidden;margin-bottom:3px;text-overflow:ellipsis;white-space:normal;overflow-wrap:break-word;-webkit-line-clamp:2;-webkit-box-orient:vertical;font-size:20px;font-weight:700"}:0);const PostItem_Date=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"ersx7z73"}:0)( true?{name:"xm5j9w",styles:"font-size:14px;font-weight:400;opacity:0.7"}:0);const Category=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"ersx7z72"}:0)( true?{name:"1bobky6",styles:"display:flex;flex-wrap:wrap;margin-top:10px;margin:10px -5px"}:0);const PostItem_CategoryItem=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"ersx7z71"}:0)( true?{name:"1bzcbme",styles:"margin:2.5px 5px;padding:3px 5px;border-radius:3px;background:black;font-size:14px;font-weight:700;color:white"}:0);const Summary=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"ersx7z70"}:0)( true?{name:"1wehmme",styles:"display:-webkit-box;overflow:hidden;margin-top:auto;text-overflow:ellipsis;white-space:normal;overflow-wrap:break-word;-webkit-line-clamp:2;-webkit-box-orient:vertical;font-size:16px;opacity:0.8"}:0);const PostItem=function({title,date,categories,summary,thumbnail:{childImageSharp:{gatsbyImageData}},link}){return (0,emotion_react_esm/* jsx */.tZ)(PostItemWrapper,{to:link},(0,emotion_react_esm/* jsx */.tZ)(ThumbnailImage,{image:gatsbyImageData,alt:"Post Item Image"}),(0,emotion_react_esm/* jsx */.tZ)(PostItemContent,null,(0,emotion_react_esm/* jsx */.tZ)(PostItem_Title,null,title),(0,emotion_react_esm/* jsx */.tZ)(PostItem_Date,null,date),(0,emotion_react_esm/* jsx */.tZ)(Category,null,categories.map(category=>(0,emotion_react_esm/* jsx */.tZ)(PostItem_CategoryItem,{key:category},category))),(0,emotion_react_esm/* jsx */.tZ)(Summary,null,summary)));};/* harmony default export */ const Main_PostItem = (PostItem);
;// CONCATENATED MODULE: ./src/hooks/useInfiniteScroll.tsx
const NUMBER_OF_ITEMS_PER_PAGE=10;const useInfiniteScroll=function(selectedCategory,posts){const containerRef=(0,index_js_.useRef)(null);const observer=(0,index_js_.useRef)(null);const{0:count,1:setCount}=(0,index_js_.useState)(1);const postListByCategory=(0,index_js_.useMemo)(()=>posts.filter(({node:{frontmatter:{categories}}})=>selectedCategory!=='All'?categories.includes(selectedCategory):true),[selectedCategory]);(0,index_js_.useEffect)(()=>{observer.current=new IntersectionObserver((entries,observer)=>{if(!entries[0].isIntersecting)return;setCount(value=>value+1);observer.unobserve(entries[0].target);});},[]);(0,index_js_.useEffect)(()=>setCount(1),[selectedCategory]);(0,index_js_.useEffect)(()=>{if(NUMBER_OF_ITEMS_PER_PAGE*count>=postListByCategory.length||containerRef.current===null||containerRef.current.children.length===0||observer.current===null)return;observer.current.observe(containerRef.current.children[containerRef.current.children.length-1]);},[count,selectedCategory]);return{containerRef,postList:postListByCategory.slice(0,count*NUMBER_OF_ITEMS_PER_PAGE)};};/* harmony default export */ const hooks_useInfiniteScroll = (useInfiniteScroll);
;// CONCATENATED MODULE: ./src/components/Main/PostList.tsx
function PostList_EMOTION_STRINGIFIED_CSS_ERROR_(){return"You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";}const PostListWrapper=/*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div", true?{target:"epn69vj0"}:0)( true?{name:"11grurp",styles:"display:grid;grid-template-columns:1fr 1fr;grid-gap:20px;width:768px;margin:0 auto;padding:50px 0 100px;@media (max-width: 768px){grid-template-columns:1fr;width:100%;padding:50px 20px;}"}:0);const PostList=function({seletedCategory,posts}){const{containerRef,postList}=hooks_useInfiniteScroll(seletedCategory,posts);return (0,emotion_react_esm/* jsx */.tZ)(PostListWrapper,{ref:containerRef},postList.map(({node:{id,fields:{slug},frontmatter}})=>(0,emotion_react_esm/* jsx */.tZ)(Main_PostItem,(0,esm_extends/* default */.Z)({},frontmatter,{link:slug,key:id}))));};/* harmony default export */ const Main_PostList = (PostList);
;// CONCATENATED MODULE: ./node_modules/decode-uri-component/index.js
const token = '%[a-f0-9]{2}';
const singleMatcher = new RegExp('(' + token + ')|([^%]+?)', 'gi');
const multiMatcher = new RegExp('(' + token + ')+', 'gi');

function decodeComponents(components, split) {
	try {
		// Try to decode the entire string first
		return [decodeURIComponent(components.join(''))];
	} catch {
		// Do nothing
	}

	if (components.length === 1) {
		return components;
	}

	split = split || 1;

	// Split the array in 2 parts
	const left = components.slice(0, split);
	const right = components.slice(split);

	return Array.prototype.concat.call([], decodeComponents(left), decodeComponents(right));
}

function decode(input) {
	try {
		return decodeURIComponent(input);
	} catch {
		let tokens = input.match(singleMatcher) || [];

		for (let i = 1; i < tokens.length; i++) {
			input = decodeComponents(tokens, i).join('');

			tokens = input.match(singleMatcher) || [];
		}

		return input;
	}
}

function customDecodeURIComponent(input) {
	// Keep track of all the replacements and prefill the map with the `BOM`
	const replaceMap = {
		'%FE%FF': '\uFFFD\uFFFD',
		'%FF%FE': '\uFFFD\uFFFD',
	};

	let match = multiMatcher.exec(input);
	while (match) {
		try {
			// Decode as big chunks as possible
			replaceMap[match[0]] = decodeURIComponent(match[0]);
		} catch {
			const result = decode(match[0]);

			if (result !== match[0]) {
				replaceMap[match[0]] = result;
			}
		}

		match = multiMatcher.exec(input);
	}

	// Add `%C2` at the end of the map to make sure it does not replace the combinator before everything else
	replaceMap['%C2'] = '\uFFFD';

	const entries = Object.keys(replaceMap);

	for (const key of entries) {
		// Replace all decoded components
		input = input.replace(new RegExp(key, 'g'), replaceMap[key]);
	}

	return input;
}

function decodeUriComponent(encodedURI) {
	if (typeof encodedURI !== 'string') {
		throw new TypeError('Expected `encodedURI` to be of type `string`, got `' + typeof encodedURI + '`');
	}

	try {
		// Try the built in decoder first
		return decodeURIComponent(encodedURI);
	} catch {
		// Fallback to a more advanced decoder
		return customDecodeURIComponent(encodedURI);
	}
}

;// CONCATENATED MODULE: ./node_modules/split-on-first/index.js
function splitOnFirst(string, separator) {
	if (!(typeof string === 'string' && typeof separator === 'string')) {
		throw new TypeError('Expected the arguments to be of type `string`');
	}

	if (string === '' || separator === '') {
		return [];
	}

	const separatorIndex = string.indexOf(separator);

	if (separatorIndex === -1) {
		return [];
	}

	return [
		string.slice(0, separatorIndex),
		string.slice(separatorIndex + separator.length)
	];
}

;// CONCATENATED MODULE: ./node_modules/filter-obj/index.js
function includeKeys(object, predicate) {
	const result = {};

	if (Array.isArray(predicate)) {
		for (const key of predicate) {
			const descriptor = Object.getOwnPropertyDescriptor(object, key);
			if (descriptor?.enumerable) {
				Object.defineProperty(result, key, descriptor);
			}
		}
	} else {
		// `Reflect.ownKeys()` is required to retrieve symbol properties
		for (const key of Reflect.ownKeys(object)) {
			const descriptor = Object.getOwnPropertyDescriptor(object, key);
			if (descriptor.enumerable) {
				const value = object[key];
				if (predicate(key, value, object)) {
					Object.defineProperty(result, key, descriptor);
				}
			}
		}
	}

	return result;
}

function excludeKeys(object, predicate) {
	if (Array.isArray(predicate)) {
		const set = new Set(predicate);
		return includeKeys(object, key => !set.has(key));
	}

	return includeKeys(object, (key, value, object) => !predicate(key, value, object));
}

;// CONCATENATED MODULE: ./node_modules/query-string/base.js




const isNullOrUndefined = value => value === null || value === undefined;

// eslint-disable-next-line unicorn/prefer-code-point
const strictUriEncode = string => encodeURIComponent(string).replaceAll(/[!'()*]/g, x => `%${x.charCodeAt(0).toString(16).toUpperCase()}`);

const encodeFragmentIdentifier = Symbol('encodeFragmentIdentifier');

function encoderForArrayFormat(options) {
	switch (options.arrayFormat) {
		case 'index': {
			return key => (result, value) => {
				const index = result.length;

				if (
					value === undefined
					|| (options.skipNull && value === null)
					|| (options.skipEmptyString && value === '')
				) {
					return result;
				}

				if (value === null) {
					return [
						...result, [encode(key, options), '[', index, ']'].join(''),
					];
				}

				return [
					...result,
					[encode(key, options), '[', encode(index, options), ']=', encode(value, options)].join(''),
				];
			};
		}

		case 'bracket': {
			return key => (result, value) => {
				if (
					value === undefined
					|| (options.skipNull && value === null)
					|| (options.skipEmptyString && value === '')
				) {
					return result;
				}

				if (value === null) {
					return [
						...result,
						[encode(key, options), '[]'].join(''),
					];
				}

				return [
					...result,
					[encode(key, options), '[]=', encode(value, options)].join(''),
				];
			};
		}

		case 'colon-list-separator': {
			return key => (result, value) => {
				if (
					value === undefined
					|| (options.skipNull && value === null)
					|| (options.skipEmptyString && value === '')
				) {
					return result;
				}

				if (value === null) {
					return [
						...result,
						[encode(key, options), ':list='].join(''),
					];
				}

				return [
					...result,
					[encode(key, options), ':list=', encode(value, options)].join(''),
				];
			};
		}

		case 'comma':
		case 'separator':
		case 'bracket-separator': {
			const keyValueSeparator = options.arrayFormat === 'bracket-separator'
				? '[]='
				: '=';

			return key => (result, value) => {
				if (
					value === undefined
					|| (options.skipNull && value === null)
					|| (options.skipEmptyString && value === '')
				) {
					return result;
				}

				// Translate null to an empty string so that it doesn't serialize as 'null'
				value = value === null ? '' : value;

				if (result.length === 0) {
					return [[encode(key, options), keyValueSeparator, encode(value, options)].join('')];
				}

				return [[result, encode(value, options)].join(options.arrayFormatSeparator)];
			};
		}

		default: {
			return key => (result, value) => {
				if (
					value === undefined
					|| (options.skipNull && value === null)
					|| (options.skipEmptyString && value === '')
				) {
					return result;
				}

				if (value === null) {
					return [
						...result,
						encode(key, options),
					];
				}

				return [
					...result,
					[encode(key, options), '=', encode(value, options)].join(''),
				];
			};
		}
	}
}

function parserForArrayFormat(options) {
	let result;

	switch (options.arrayFormat) {
		case 'index': {
			return (key, value, accumulator) => {
				result = /\[(\d*)]$/.exec(key);

				key = key.replace(/\[\d*]$/, '');

				if (!result) {
					accumulator[key] = value;
					return;
				}

				if (accumulator[key] === undefined) {
					accumulator[key] = {};
				}

				accumulator[key][result[1]] = value;
			};
		}

		case 'bracket': {
			return (key, value, accumulator) => {
				result = /(\[])$/.exec(key);
				key = key.replace(/\[]$/, '');

				if (!result) {
					accumulator[key] = value;
					return;
				}

				if (accumulator[key] === undefined) {
					accumulator[key] = [value];
					return;
				}

				accumulator[key] = [...accumulator[key], value];
			};
		}

		case 'colon-list-separator': {
			return (key, value, accumulator) => {
				result = /(:list)$/.exec(key);
				key = key.replace(/:list$/, '');

				if (!result) {
					accumulator[key] = value;
					return;
				}

				if (accumulator[key] === undefined) {
					accumulator[key] = [value];
					return;
				}

				accumulator[key] = [...accumulator[key], value];
			};
		}

		case 'comma':
		case 'separator': {
			return (key, value, accumulator) => {
				const isArray = typeof value === 'string' && value.includes(options.arrayFormatSeparator);
				const isEncodedArray = (typeof value === 'string' && !isArray && base_decode(value, options).includes(options.arrayFormatSeparator));
				value = isEncodedArray ? base_decode(value, options) : value;
				const newValue = isArray || isEncodedArray ? value.split(options.arrayFormatSeparator).map(item => base_decode(item, options)) : (value === null ? value : base_decode(value, options));
				accumulator[key] = newValue;
			};
		}

		case 'bracket-separator': {
			return (key, value, accumulator) => {
				const isArray = /(\[])$/.test(key);
				key = key.replace(/\[]$/, '');

				if (!isArray) {
					accumulator[key] = value ? base_decode(value, options) : value;
					return;
				}

				const arrayValue = value === null
					? []
					: value.split(options.arrayFormatSeparator).map(item => base_decode(item, options));

				if (accumulator[key] === undefined) {
					accumulator[key] = arrayValue;
					return;
				}

				accumulator[key] = [...accumulator[key], ...arrayValue];
			};
		}

		default: {
			return (key, value, accumulator) => {
				if (accumulator[key] === undefined) {
					accumulator[key] = value;
					return;
				}

				accumulator[key] = [...[accumulator[key]].flat(), value];
			};
		}
	}
}

function validateArrayFormatSeparator(value) {
	if (typeof value !== 'string' || value.length !== 1) {
		throw new TypeError('arrayFormatSeparator must be single character string');
	}
}

function encode(value, options) {
	if (options.encode) {
		return options.strict ? strictUriEncode(value) : encodeURIComponent(value);
	}

	return value;
}

function base_decode(value, options) {
	if (options.decode) {
		return decodeUriComponent(value);
	}

	return value;
}

function keysSorter(input) {
	if (Array.isArray(input)) {
		return input.sort();
	}

	if (typeof input === 'object') {
		return keysSorter(Object.keys(input))
			.sort((a, b) => Number(a) - Number(b))
			.map(key => input[key]);
	}

	return input;
}

function removeHash(input) {
	const hashStart = input.indexOf('#');
	if (hashStart !== -1) {
		input = input.slice(0, hashStart);
	}

	return input;
}

function getHash(url) {
	let hash = '';
	const hashStart = url.indexOf('#');
	if (hashStart !== -1) {
		hash = url.slice(hashStart);
	}

	return hash;
}

function parseValue(value, options) {
	if (options.parseNumbers && !Number.isNaN(Number(value)) && (typeof value === 'string' && value.trim() !== '')) {
		value = Number(value);
	} else if (options.parseBooleans && value !== null && (value.toLowerCase() === 'true' || value.toLowerCase() === 'false')) {
		value = value.toLowerCase() === 'true';
	}

	return value;
}

function extract(input) {
	input = removeHash(input);
	const queryStart = input.indexOf('?');
	if (queryStart === -1) {
		return '';
	}

	return input.slice(queryStart + 1);
}

function parse(query, options) {
	options = {
		decode: true,
		sort: true,
		arrayFormat: 'none',
		arrayFormatSeparator: ',',
		parseNumbers: false,
		parseBooleans: false,
		...options,
	};

	validateArrayFormatSeparator(options.arrayFormatSeparator);

	const formatter = parserForArrayFormat(options);

	// Create an object with no prototype
	const returnValue = Object.create(null);

	if (typeof query !== 'string') {
		return returnValue;
	}

	query = query.trim().replace(/^[?#&]/, '');

	if (!query) {
		return returnValue;
	}

	for (const parameter of query.split('&')) {
		if (parameter === '') {
			continue;
		}

		const parameter_ = options.decode ? parameter.replaceAll('+', ' ') : parameter;

		let [key, value] = splitOnFirst(parameter_, '=');

		if (key === undefined) {
			key = parameter_;
		}

		// Missing `=` should be `null`:
		// http://w3.org/TR/2012/WD-url-20120524/#collect-url-parameters
		value = value === undefined ? null : (['comma', 'separator', 'bracket-separator'].includes(options.arrayFormat) ? value : base_decode(value, options));
		formatter(base_decode(key, options), value, returnValue);
	}

	for (const [key, value] of Object.entries(returnValue)) {
		if (typeof value === 'object' && value !== null) {
			for (const [key2, value2] of Object.entries(value)) {
				value[key2] = parseValue(value2, options);
			}
		} else {
			returnValue[key] = parseValue(value, options);
		}
	}

	if (options.sort === false) {
		return returnValue;
	}

	// TODO: Remove the use of `reduce`.
	// eslint-disable-next-line unicorn/no-array-reduce
	return (options.sort === true ? Object.keys(returnValue).sort() : Object.keys(returnValue).sort(options.sort)).reduce((result, key) => {
		const value = returnValue[key];
		result[key] = Boolean(value) && typeof value === 'object' && !Array.isArray(value) ? keysSorter(value) : value;
		return result;
	}, Object.create(null));
}

function stringify(object, options) {
	if (!object) {
		return '';
	}

	options = {
		encode: true,
		strict: true,
		arrayFormat: 'none',
		arrayFormatSeparator: ',',
		...options,
	};

	validateArrayFormatSeparator(options.arrayFormatSeparator);

	const shouldFilter = key => (
		(options.skipNull && isNullOrUndefined(object[key]))
		|| (options.skipEmptyString && object[key] === '')
	);

	const formatter = encoderForArrayFormat(options);

	const objectCopy = {};

	for (const [key, value] of Object.entries(object)) {
		if (!shouldFilter(key)) {
			objectCopy[key] = value;
		}
	}

	const keys = Object.keys(objectCopy);

	if (options.sort !== false) {
		keys.sort(options.sort);
	}

	return keys.map(key => {
		const value = object[key];

		if (value === undefined) {
			return '';
		}

		if (value === null) {
			return encode(key, options);
		}

		if (Array.isArray(value)) {
			if (value.length === 0 && options.arrayFormat === 'bracket-separator') {
				return encode(key, options) + '[]';
			}

			return value
				.reduce(formatter(key), [])
				.join('&');
		}

		return encode(key, options) + '=' + encode(value, options);
	}).filter(x => x.length > 0).join('&');
}

function parseUrl(url, options) {
	options = {
		decode: true,
		...options,
	};

	let [url_, hash] = splitOnFirst(url, '#');

	if (url_ === undefined) {
		url_ = url;
	}

	return {
		url: url_?.split('?')?.[0] ?? '',
		query: parse(extract(url), options),
		...(options && options.parseFragmentIdentifier && hash ? {fragmentIdentifier: base_decode(hash, options)} : {}),
	};
}

function stringifyUrl(object, options) {
	options = {
		encode: true,
		strict: true,
		[encodeFragmentIdentifier]: true,
		...options,
	};

	const url = removeHash(object.url).split('?')[0] || '';
	const queryFromUrl = extract(object.url);

	const query = {
		...parse(queryFromUrl, {sort: false}),
		...object.query,
	};

	let queryString = stringify(query, options);
	queryString &&= `?${queryString}`;

	let hash = getHash(object.url);
	if (typeof object.fragmentIdentifier === 'string') {
		const urlObjectForFragmentEncode = new URL(url);
		urlObjectForFragmentEncode.hash = object.fragmentIdentifier;
		hash = options[encodeFragmentIdentifier] ? urlObjectForFragmentEncode.hash : `#${object.fragmentIdentifier}`;
	}

	return `${url}${queryString}${hash}`;
}

function pick(input, filter, options) {
	options = {
		parseFragmentIdentifier: true,
		[encodeFragmentIdentifier]: false,
		...options,
	};

	const {url, query, fragmentIdentifier} = parseUrl(input, options);

	return stringifyUrl({
		url,
		query: includeKeys(query, filter),
		fragmentIdentifier,
	}, options);
}

function exclude(input, filter, options) {
	const exclusionFilter = Array.isArray(filter) ? key => !filter.includes(key) : (key, value) => !filter(key, value);

	return pick(input, exclusionFilter, options);
}

;// CONCATENATED MODULE: ./node_modules/query-string/index.js


/* harmony default export */ const query_string = (base_namespaceObject);

;// CONCATENATED MODULE: ./src/pages/index.tsx

function pages_EMOTION_STRINGIFIED_CSS_ERROR_() {
  return "You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";
}







const Container = /*#__PURE__*/(0,emotion_styled_base_esm/* default */.Z)("div",  true ? {
  target: "e1s0d83s0"
} : 0)( true ? {
  name: "zyllbl",
  styles: "display:flex;flex-direction:column;height:100vh"
} : 0);
const IndexPage = function ({
  location: {
    search
  },
  data: {
    site: {
      siteMetadata: {
        title,
        description,
        siteUrl
      }
    },
    allMarkdownRemark: {
      edges
    },
    file: {
      childImageSharp: {
        gatsbyImageData
      },
      publicURL
    }
  }
}) {
  const parsed = query_string.parse(search);
  const selectedCategory = typeof parsed.category !== 'string' || !parsed.category ? 'All' : parsed.category;
  const categoryList = (0,index_js_.useMemo)(() => edges.reduce((list, {
    node: {
      frontmatter: {
        categories
      }
    }
  }) => {
    categories.forEach(category => {
      if (list[category] === undefined) list[category] = 1;else list[category]++;
    });
    list['All']++;
    return list;
  }, {
    All: 0
  }), []);
  return (0,emotion_react_esm/* jsx */.tZ)(Template/* default */.Z, {
    title: title,
    description: description,
    url: siteUrl,
    image: publicURL
  }, (0,emotion_react_esm/* jsx */.tZ)(Main_Introduction, {
    profileImage: gatsbyImageData
  }), (0,emotion_react_esm/* jsx */.tZ)(Main_CategoryList, {
    selectedCategory: selectedCategory,
    categoryList: categoryList
  }), (0,emotion_react_esm/* jsx */.tZ)(Main_PostList, {
    seletedCategory: selectedCategory,
    posts: edges
  }));
};
/* harmony default export */ const pages = (IndexPage);
const getPostList = "3760508704";

/***/ })

};
;
//# sourceMappingURL=component---src-pages-index-tsx.js.map